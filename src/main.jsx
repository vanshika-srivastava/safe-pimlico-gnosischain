import React from 'react';
import ReactDOM from 'react-dom';
import App from './safe-pimlico.jsx';
import './index.css';

// Use ReactDOM.createRoot to render your app
ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
